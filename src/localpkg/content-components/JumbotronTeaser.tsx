import React from 'react'; 

const JumbotronTeaser: React.FC = ()=>{
    return(
        <h1>
            Jumbotron Content Teaser
        </h1>
    );
};

export default React.memo(JumbotronTeaser); 